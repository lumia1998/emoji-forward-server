version: '3'

services:
  image-server:
    build: .
    ports:
      - "46667:6667"
    volumes:
      - ./image_collections:/app/image_collections
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
      - PYTHONDONTWRITEBYTECODE=1
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:6667/status"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
